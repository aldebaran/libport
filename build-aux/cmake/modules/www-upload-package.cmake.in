message("Uploading '${CPACK_PACKAGE_FULL_FILE_NAME}'...")
execute_process(
  COMMAND
  scp -q
  "${CPACK_PACKAGE_FULL_FILE_NAME}"
  "${WWW_UPLOAD_PACKAGE_URL}"
  WORKING_DIRECTORY ${CMAKE_BINARY_DIR}
  )
message("Upgrading index page...")
execute_process(
  COMMAND
  scp -q
  "www-upload-package.index.html.php"
  "${WWW_UPLOAD_PACKAGE_URL}/index.html.php"
  WORKING_DIRECTORY ${CMAKE_BINARY_DIR}
  )
